#include<stdio.h>
int main()
{
  int n, num, rev=0;
  printf("enter a number=");
  scanf("%d", &n);
  n=num;
  while(num>0)
    {
    rev=rev*10+(num%10);
    num/=10;
    }
  printf("reversed number= %d\n", rev);
  return 0;
  }
